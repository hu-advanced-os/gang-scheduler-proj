
README

BUILD AND RUN
=============

to execute use the following files.

cd $projectDir.

./c to compile.

./r to run an example.

./h to see help page.

(NOTE: cd to the bin directory to run these. these scripts use unix filepaths).

ISSUES (Burge)

ISSUES
======

1. calculation of default bounded standard deviation for -num-tasks-std

2. large values can be returned by normal distributions. the maximum value used is Integer.MAX_INT. i.e. still very large. 

